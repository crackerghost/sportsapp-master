<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="style.css">
     <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
        
 
</head>
<body>
 <!-- navbar -->
    <nav class="navbar navbar-expand-lg bg-transparent fixed-top">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Logo</a>

        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarText"
          aria-controls="navbarText"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
          <span class="navbar-text">
            <a
              class="nav-link active"
              style=" cursor:pointer; color: white; margin-right: 20px"
              aria-current="page"
              onclick="location.href='/'"
              >Home</a
            >
            <button class="loginbtn" onclick="location.href='/login2'">Login</button>
          </span>
        </div>
      </div>
    </nav>
{{!-- navbar --}}
    

    <div class="login-form">
        <h1>Login</h1>
        <form class="loginForm" action="/login" method="POST">
            <input type="email" style="margin: 5px;" name="email" id="email" placeholder="Email" required>
            <input type="password" style="margin: 5px;" name="password" id="password" placeholder="Password" required>
            <button type="submit" style="margin: 5px;" class="login-btn" onclick="showregister()">Login</button>
            
        </form>
    </div>
      <script src="../../server/app.js"></script>
    <script src="script.js"></script>
        <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <script>
       
        function showregister(){
            const emailElement = document.getElementsByClassName('email')[0];
            const emailValue = emailElement ? emailElement.value : '';

        // Set the cookie with a name, value, and optional expiration date
            document.cookie = `email=${emailValue}; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/`;

            console.log('Email saved as cookie:', emailValue);
         
        }
    </script>

    <script>
    async function checkAuthStatus() {
      try {
        const response = await fetch('/check-auth');
        const data = await response.json();

        if (data.authenticated) {
           window.location.href = '/homepage'
         
        } else {

            


          
        }
      } catch (error) {
        console.error('Error checking authentication status:', error);
      }
    }

    async function login() {
      // Perform your login logic, e.g., redirect to the login page
    }

    async function logout() {
      // Perform your logout logic, e.g., redirect to the logout page
    }

    // Check authentication status when the page loads
    window.onload = checkAuthStatus;
  </script>
</body>
</html>